import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFFFFAF5);

  static const Color primary = Color(0xFFDF3030);

  //A contrast color for text/icons on primary color
  static const Color onPrimary = Color(0xFFFFFFFF);

  // A standard text color
  static const Color text = Color(0xFF212121);

  // A light grey for borders or disabled states
  static const Color textSecondary = Color(0xFF757575);
  static const Color border = Color(0xFFBDBDBD);
}